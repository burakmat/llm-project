services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - 6333:6333
    volumes:
      - qdrant-volume:/qdrant/storage
  django:
    build: ./backend/
    ports:
      - 8000:8000
    depends_on:
      - qdrant
    volumes:
      - ./backend:/app

volumes:
  qdrant-volume: